/**
 * change-requests.js — desktop change requests screen stub
 */
export default async function render(root, params = {}) {
  root.innerHTML = '<div class="screen" style="padding: var(--pad)"><p style="color: var(--ink-fade); font-size: 14px">Change Requests — coming soon</p></div>';
}
