// screens/mobile/change-requests.js
export default async function render(root, params = {}) {
  root.innerHTML = `
    <div class="screen" style="padding: var(--pad)">
      <p style="color: var(--ink-fade); font-size: 14px">Mobile Change Requests — coming soon</p>
    </div>
  `;
}
